"use client";

import DashboardPage from "@/components/dashboard-page";

export default function Dashboard() {
  return <DashboardPage />;
}
try {
  // lógica
} catch (error) {
  console.error("🔥 Error en página dashboard:", error);
}
            